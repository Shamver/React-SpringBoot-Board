<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="kr.co.board.mapper.FamilyMapper">

    <select id="familyList" resultType="FamilyVO">
        SELECT id, name, rel_Ship as relShip FROM FAMILY
    </select>

    <insert id="familyInsert" parameterType="FamilyVO">
        INSERT INTO FAMILY VALUES(
        (SELECT NVL(MAX(id)+1,0) FROM FAMILY),
        '${name}',
        '${relShip}'
        )
    </insert>

    <update id="familyUpdate" parameterType="FamilyVO">
        UPDATE FAMILY SET
        name='${name}',
        rel_ship='${relShip}'
        WHERE id=${id}
    </update>

    <delete id="familyDelete" parameterType="FamilyVO">
        DELETE FROM FAMILY
        WHERE id=${id}
    </delete>


</mapper>